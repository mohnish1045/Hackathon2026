import { getPlannerItems } from "@/lib/data";
export default async function TasksPage() { const tasks = (await getPlannerItems()).filter((item) => item.type === "task" || item.type === "assignment"); return <main className="min-h-screen bg-slate-50 p-6 md:p-10"><div className="mx-auto max-w-3xl"><a href="/planner" className="text-sm font-medium text-indigo-600">← Back to planner</a><h1 className="mt-6 text-3xl font-bold">Tasks and assignments</h1><p className="mt-2 text-slate-600">Everything that needs your attention, ordered by deadline.</p><div className="mt-7 space-y-3">{tasks.map((task) => <article key={task.id} className="flex items-center justify-between rounded-2xl border bg-white p-5"><div><p className="font-semibold">{task.title}</p><p className="mt-1 text-sm text-slate-500">{task.course || "Personal"} · {task.dueAt ? `Due ${new Date(task.dueAt).toLocaleString()}` : "No due date"}</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${task.priority === "high" ? "bg-rose-100 text-rose-700" : "bg-slate-100 text-slate-600"}`}>{task.priority}</span></article>)}</div></div></main>; }
